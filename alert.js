if (navigator.userAgent.indexOf('Safari', 'iPhone', 'iPod') !== -1 && navigator.userAgent.indexOf('Chrome') === -1) {
    alert("Some features that work on some browsers do not always transfer over. There are several bugs identified and actively being fixed like the infinite load with Safari and the Peak bar not showing proper values.")
}